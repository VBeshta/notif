name: flutter workflow

on:
  push:
    branches: [ "master", "develop" ]
  pull_request:
    branches: [ "master", "develop" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.16.0'
          channel: 'stable'
      - name: Install dependencies
        run: flutter pub get

      - name: Verify formatting
        run: dart format --output=none --set-exit-if-changed .

      - name: Analyze project source
        run: dart analyze

      # Your project will need to have tests in test/ and a dependency on
      # package:test for this step to succeed. Note that Flutter projects will
      # want to change this to 'flutter test'.
      - name: Run tests
        run: flutter test


    #     # Create android apk
    #   - name: Build apk
    #     run: flutter build apk

    # [article](https://steveos.medium.com/github-action-and-firebase-app-distribution-ci-cd-ways-part-2-fcf9ba425c0) 
    # to generate a Credential file from Google Cloud Console.
    #     # Upload apk to Firebase distribution
    #   - name: Upload apk to Firebase distribution
    #     uses: wzieba/Firebase-Distribution-Github-Action@v1.7.0
    #     with:
    #         appId: ${{ secrets.FIREBASE_APP_ID_ANDROID }}
    #         serviceCredentialsFileContent: ${{ secrets.FIREBASE_TOKEN }}
    #         groups: all_testers
    #         file: build/app/outputs/flutter-apk/app-release.apk